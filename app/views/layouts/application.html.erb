<!DOCTYPE html>
<html>
<head>
  <title>Facewall</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<% if current_user %>
    <% request_count = current_user.requested_friends.count %>
    <% request_count == 0 ? link = "#{current_user.full_name}" : link = "#{current_user.full_name} [#{request_count}]" %>
<% end %>

<div class="collapse navbar-inverse navbar-collapse" id="navbar-collapse-1" style="">
  <% if user_signed_in? %>
      <ul class="nav navbar-nav navbar-left">
        <li><%= link_to ' FaceWall', root_path, class: 'stronger glyphicon glyphicon-home', style: 'line-height: normal;' %></i></li>
        <li><%= link_to 'Users', users_index_path %></li>
      </ul>

      <!-- TODO Vzhledavani v prispevcivh ne v uyivatelich-->
      <%= form_tag(users_index_path, :method => 'get', class: 'col-sm-3 col-md-3 nav navbar-nav navbar-left head-search') do %>
          <div class="input-group">
            <%= text_field_tag :search, params[:search], class: 'form-control', placeholder: 'Find post' %>
            <div class="input-group-btn">
              <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
            </div>
          </div>
      <% end %>

      <ul class="nav navbar-nav navbar-right" style="margin-right: 0px;">
        <li class="image round-image-40"><%= image_tag(current_user.avatar.url(:thumb)) %></li>
        <li class="stronger"><%= link_to link, user_path(current_user.id) %></li>
        <li><%= link_to 'Logout', destroy_user_session_path, method: :delete %></li>
      </ul>
  <% end %>
</div>

<div id='wrapper'>
  <% if notice %>
      <p class="alert alert-success" id="notice"><%= notice %></p>
  <% end %>
  <% if alert %>
      <p class="alert alert-danger" id="alert"><%= alert %></p>
  <% end %>

  <%= yield %>
</div>

<!--<div class='content-block' id="footer">-->
<!--&lt;!&ndash;FaceWall &copy; <%= Time.now.year %>, Tomas Kubicek&ndash;&gt;-->
<!--</div>-->

</body>
</html>
