<!DOCTYPE html>
<html>
<head>
  <title>Facewall</title>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="collapse navbar-inverse navbar-collapse" id="navbar-collapse-1" style="">
  <% if user_signed_in? %>
      <ul class="nav navbar-nav navbar-left">
        <li><%= link_to ' Facewall', root_path, class: 'stronger glyphicon glyphicon-home', style: 'line-height: normal;' %></i></li>
        <li><%= link_to 'Uživatelé', users_index_path %></li>
      </ul>

<!-- TODO Vzhledavani v prispevcivh ne v uyivatelich-->
      <%= form_tag(users_index_path, :method => 'get', class: 'col-sm-3 col-md-3 nav navbar-nav navbar-left search') do %>
          <div class="input-group">
            <%= text_field_tag :search, params[:search], class: 'form-control', placeholder: 'Najít příspěvek' %>
            <div class="input-group-btn">
              <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
            </div>
          </div>
      <% end %>

      <ul class="nav navbar-nav navbar-right" style="margin-right: 0px;">
        <li class="image round-image-40"><%= image_tag(current_user.avatar.url(:thumb)) %></li>
        <li class="stronger"><%= link_to "#{current_user.full_name}", '#' %></li>
        <li><%= link_to 'Upravit profil', edit_user_registration_path %></li>
        <li><%= link_to 'Odhlásit', destroy_user_session_path, method: :delete %></li>
      </ul>
  <% end %>
</div>

<% if notice %>
    <p class="alert alert-success" id="notice"><%= notice %></p>
<% end %>
<% if alert %>
    <p class="alert alert-danger" id="alert"><%= alert %></p>
<% end %>

<%= yield %>

</body>
</html>
