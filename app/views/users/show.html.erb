<%= render 'cover_and_profile' %>
<%= render 'friends' %>

<%= link_to 'asdas',
            new_gallery_path,
            :class => 'btn btn-primary' %>

<% @user.galleries.each do |gallery| %>
    <% gallery.gallery_images.each do |image| %>
        <img class="profile" src="<%= image.photo %>"/>
    <% end %>
<% end %>
